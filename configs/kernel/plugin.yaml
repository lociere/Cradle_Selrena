# 插件系统配置
plugin_root_dir: "plugins"

sandbox:
  enable_isolation: true
  timeout_ms: 5000
  allow_native_modules: false

default_permissions:
  - CHAT_SEND
  - MEMORY_READ
  - MEMORY_WRITE
  - CONFIG_READ_SELF
  - CONFIG_WRITE_SELF
  - CONFIG_READ_GLOBAL
  - EVENT_SUBSCRIBE

plugin_blacklist: []
